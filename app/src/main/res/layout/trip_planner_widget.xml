<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android" android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/white"
    android:id="@+id/trip_planner_widget"
    android:clickable="true"
    >
    <TextView
        android:id="@+id/time_remaining_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:text="Time remaining until the date of your Trip:"
        android:textStyle="bold"
        android:textColor="@color/black"
        />
    <TextView
        android:id="@+id/time_count_down"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="day:hours:minutes:seconds"
        android:layout_centerInParent="true"
        android:layout_below="@+id/time_remaining_label"
        android:textColor="@color/black"
    />
</RelativeLayout>